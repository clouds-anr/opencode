name: release
run-name: "${{ (github.event_name == 'push' && 'release patch') || (inputs.bump && format('release {0}', inputs.bump)) || format('release {0}', github.ref_name) }}"

on:
  push:
    branches:
      - dev
  workflow_dispatch:
    inputs:
      bump:
        description: "Bump major, minor, or patch (leave empty for preview)"
        required: false
        type: choice
        options:
          - ""
          - patch
          - minor
          - major
      version:
        description: "Override version (optional)"
        required: false
        type: string

concurrency: ${{ github.workflow }}-${{ github.ref }}-${{ inputs.version || inputs.bump || github.sha }}

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  # Release gate (clouds-anr/opencode#307, phase 5): no release is cut unless the
  # deterministic test gate is green for this exact commit. `version` creates the
  # draft GitHub release/tag and every build job depends on it, so a red gate
  # yields no tag, no binaries, no release. Direct job dependency — no fragile
  # cross-workflow workflow_run polling. Intentionally typecheck + `bun turbo
  # test` only (not Playwright e2e or the multi-OS smoke matrix) to keep release
  # blocking deterministic and fast.
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "24"
      - uses: ./.github/actions/setup-bun
      - name: Typecheck (full workspace)
        run: bun turbo typecheck
      - name: Run tests (full workspace)
        run: GITHUB_ACTIONS=false bun turbo test

  # ---------------------------------------------------------------------------
  # 1. Version: derive from git tags, create a draft GitHub release
  # ---------------------------------------------------------------------------
  version:
    needs: tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          fetch-tags: true

      - uses: ./.github/actions/setup-bun

      - name: Setup git committer
        id: committer
        uses: ./.github/actions/setup-git-committer
        with:
          opencode-app-id: ${{ vars.OPENCODE_APP_ID }}
          opencode-app-secret: ${{ secrets.OPENCODE_APP_SECRET }}

      - name: Install OpenCode
        if: inputs.bump || inputs.version || github.event_name == 'push'
        run: bun i -g opencode-ai

      - id: version
        run: ./script/version-tag.ts
        env:
          GH_TOKEN: ${{ steps.committer.outputs.token }}
          OPENCODE_BUMP: ${{ inputs.bump || (github.event_name == 'push' && 'patch') }}
          OPENCODE_VERSION: ${{ inputs.version }}
          OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
          GH_REPO: ${{ (github.ref_name == 'beta' && 'anomalyco/opencode-beta') || github.repository }}
    outputs:
      version: ${{ steps.version.outputs.version }}
      release: ${{ steps.version.outputs.release }}
      tag: ${{ steps.version.outputs.tag }}
      repo: ${{ steps.version.outputs.repo }}

  # ---------------------------------------------------------------------------
  # 2. Build CLI: compile single-file executables for all platform targets
  # ---------------------------------------------------------------------------
  build-cli:
    needs: version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-tags: true

      - uses: ./.github/actions/setup-bun

      - name: Setup git committer
        id: committer
        uses: ./.github/actions/setup-git-committer
        with:
          opencode-app-id: ${{ vars.OPENCODE_APP_ID }}
          opencode-app-secret: ${{ secrets.OPENCODE_APP_SECRET }}

      - name: Build
        id: build
        run: ./packages/opencode/script/build.ts
        env:
          OPENCODE_VERSION: ${{ needs.version.outputs.version }}
          OPENCODE_RELEASE: ${{ needs.version.outputs.release }}
          GH_REPO: ${{ needs.version.outputs.repo }}
          GH_TOKEN: ${{ steps.committer.outputs.token }}

      - uses: actions/upload-artifact@v4
        with:
          name: opencode-cli
          path: packages/opencode/dist
    outputs:
      version: ${{ needs.version.outputs.version }}
